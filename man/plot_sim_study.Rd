% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_sim_study}
\alias{plot_sim_study}
\title{Plot Monte Carlo results across the true number of effects}
\usage{
plot_sim_study(x, y_var = "var_ratio", ...)
}
\arguments{
\item{x}{a data frame of \code{\link[=sim_study]{sim_study()}} results, stacked across \code{m_star}.}

\item{y_var}{which column to plot.}

\item{...}{unused.}
}
\value{
\code{x}, invisibly.
}
\description{
Figure 1: average sampling variance of the cohort-time estimates against
\eqn{m^*}. The feasible estimators track the infeasible oracle in the
partial-homogeneity region and carry a selection overhead when every cell is
genuinely distinct.
}
\examples{
\donttest{
des <- ph_design(N = 300, T = 6, cohorts = c(0, 3, 5))
res <- do.call(rbind, lapply(c(1, 3), function(m) {
  sim_study(des, m_star = m, delta = 6, R = 5,
            methods = c("flexible", "oracle"), progress = FALSE, seed = m)
}))
plot_sim_study(res)
}
}
