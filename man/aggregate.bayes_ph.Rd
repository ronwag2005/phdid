% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate.bayes_ph}
\alias{aggregate.bayes_ph}
\alias{aggregate.ph_fit}
\title{Aggregate cohort-time effects into a reported estimand}
\usage{
\method{aggregate}{bayes_ph}(
  x,
  type = c("overall", "dynamic", "group", "calendar"),
  weights = NULL,
  level = 0.95,
  ...
)

\method{aggregate}{ph_fit}(
  x,
  type = c("overall", "dynamic", "group", "calendar"),
  weights = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{x}{a \link{bayes_ph}, \link{l0_ph} or \link{ph_fit} object.}

\item{type}{the aggregation scheme. \code{"overall"} uses the weights carried by
the \link{ph_data} object (eq. 2). \code{"dynamic"} averages within event time
\eqn{t - g}, the event-study profile. \code{"group"} averages within cohort,
\code{"calendar"} within calendar period. \code{"dynamic"}, \code{"group"} and
\code{"calendar"} need cohort and time labels on the cells.}

\item{weights}{optional numeric vector of length K overriding the object's
weights, or a K x J matrix whose columns are J separate aggregators.}

\item{level}{credible / confidence level.}

\item{...}{unused.}
}
\value{
A data frame with one row per reported estimand, holding the
estimate, standard error or posterior standard deviation, and interval
bounds.
}
\description{
The cohort-time effects are rarely the final object; papers report an
overall ATT, an event-study profile, or effects by cohort. All of these are
linear aggregators \eqn{\sum_k w_k \tau_k} of the cell effects, so the
specification question is orthogonal to the choice of aggregator.
}
\details{
For a \link{bayes_ph} posterior the aggregator is applied to \strong{each draw} and
summarised across the chain. This is the point of the Bayesian route: the
reported interval propagates uncertainty about the partition itself, not
merely sampling uncertainty given a grouping.

For an \link{l0_ph} or \link{ph_fit} object the aggregate is computed from the fitted
effects with the plug-in variance \eqn{w' R S^{-1} R' w}, which conditions on
the selected partition being correct. Remark 3 of the paper is the caveat.
}
\examples{
d <- ph_data(c(0.10, 0.11, 0.42, 0.40), Sigma = 0.02^2 * (0.3 + 0.7 * diag(4)),
             cells = c("2:2", "2:3", "3:3", "3:4"))
fit <- bayes_ph(d, alpha = 1, iters = 400, burn = 100, seed = 1)
aggregate(fit, "overall")
aggregate(fit, "dynamic")
}
\references{
Arora, P. and Wagle, R. (2026). Section 3.3 and Remark 3.
See \code{citation("phdid")} for the full reference.
}
