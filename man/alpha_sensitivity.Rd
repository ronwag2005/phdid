% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{alpha_sensitivity}
\alias{alpha_sensitivity}
\title{Sensitivity of the posterior to the concentration parameter}
\usage{
alpha_sensitivity(
  object,
  alpha_grid = c(0.1, 0.25, 0.5, 1, 2, 5, 10, 25, 50, 100),
  type = "overall",
  ...,
  seed = NULL
)
}
\arguments{
\item{object}{a \link{ph_data} object.}

\item{alpha_grid}{concentration values to trace.}

\item{type}{aggregation passed to \code{\link[=aggregate.bayes_ph]{aggregate.bayes_ph()}}.}

\item{...}{further arguments to \code{\link[=bayes_ph]{bayes_ph()}}, such as \code{iters} and \code{burn}.}

\item{seed}{optional integer for reproducibility.}
}
\value{
A data frame with one row per \code{alpha}, holding the aggregate
estimate, its credible bounds, and the posterior expected number of
groups. Plot it with \code{\link[=plot_alpha_sensitivity]{plot_alpha_sensitivity()}}.
}
\description{
Refits the Dirichlet Process model across a grid of \code{alpha} and traces the
overall effect, its credible band, and the posterior expected number of
groups. This is Figure 5 of the paper.
}
\details{
Reporting the path rather than a single \code{alpha} is the paper's own
recommendation, and for a good reason. In its simulation the path is nearly
flat, so the choice is immaterial; but in its seven-cell application the
overall effect slides from about -0.017 under heavy pooling to about -0.038
as the model approaches the flexible fit. Choosing \code{alpha} from the data
would turn the prior into a data-dependent object, with the usual
empirical-Bayes consequences of understated posterior uncertainty and a
double use of the data, so the honest report is the whole path.
}
\examples{
d <- ph_data(c(0.10, 0.11, 0.42, 0.40), Sigma = 0.02^2 * (0.3 + 0.7 * diag(4)))
alpha_sensitivity(d, alpha_grid = c(0.5, 1, 5), iters = 400, burn = 100,
                  seed = 1)
}
