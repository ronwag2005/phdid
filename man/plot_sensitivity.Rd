% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_sensitivity}
\alias{plot_sensitivity}
\title{Plot a regularisation path}
\usage{
plot_sensitivity(
  x,
  term = NULL,
  band = NULL,
  label = TRUE,
  col = c("#1b9e77", "#d95f02", "#7570b3", "#e7298a", "#66a61e", "#e6ab02", "#a6761d"),
  main = NULL,
  ...
)
}
\arguments{
\item{x}{a data frame from \code{\link[=alpha_sensitivity]{alpha_sensitivity()}} or \code{\link[=lambda_sensitivity]{lambda_sensitivity()}}.}

\item{term}{for an aggregate table with several terms, which one to plot.}

\item{band}{draw the interval band. On a per-cell plot with many cells the
bands overlap badly, so the default omits them there.}

\item{label}{write the cell name at the right edge of each line.}

\item{col}{a vector of line colours, recycled across cells.}

\item{main}{plot title.}

\item{...}{passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
Draws the table returned by \code{\link[=alpha_sensitivity]{alpha_sensitivity()}} or \code{\link[=lambda_sensitivity]{lambda_sensitivity()}}.
The two views answer different questions.
}
\details{
With an aggregate table (\code{type = "overall"} and friends) the top panel shows
the estimate and its band against the fully pooled and fully flexible
anchors, and the lower panel the number of groups. Where the path is flat the
tuning parameter does not matter; where it slides, report the path rather
than a single value.

With a per-cell table (\code{type = "cells"}) each cohort-time effect gets its own
line, drawn from its flexible value on the loose-penalty side toward the
pooled value as the penalty tightens. Lines that converge are cells the
method is merging, and where they meet is the penalty at which it does so.
This is the view that shows what a flat aggregate path can hide: the overall
ATT is robust to over-pooling, while the individual effects are not.
}
\examples{
d <- ph_data(c(0.10, 0.11, 0.42, 0.40), Sigma = 0.02^2 * (0.3 + 0.7 * diag(4)))
plot_sensitivity(lambda_sensitivity(d, type = "cells"))
}
