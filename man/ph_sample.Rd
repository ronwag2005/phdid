% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{ph_sample}
\alias{ph_sample}
\title{Draw one replication from a partial-homogeneity design}
\usage{
ph_sample(design, tau_true)
}
\arguments{
\item{design}{a \link{ph_design} object.}

\item{tau_true}{the true cohort-time effects, or a \code{\link[=ph_truth]{ph_truth()}} list.}
}
\value{
A \link{ph_data} object.
}
\description{
Generates within-transformed outcomes under the given true effects, fits the
fully flexible model, and returns the result as a \link{ph_data} object ready for
any estimator in the package. Unit and time fixed effects are removed by the
transformation, so the sampling behaviour is driven entirely by the errors,
as in eq. (40).
}
\examples{
des <- ph_design(N = 300, T = 6, cohorts = c(0, 3, 5))
truth <- ph_truth(des, m_star = 2, delta = 6)
d <- ph_sample(des, truth)
l0_ph(d)$m
}
