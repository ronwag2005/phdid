% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{bayes_ph}
\alias{bayes_ph}
\title{The Dirichlet Process partial-homogeneity estimator}
\usage{
bayes_ph(
  object,
  alpha = 1,
  mu0 = NULL,
  sigma0_sq = NULL,
  iters = 4000L,
  burn = 1000L,
  thin = 1L,
  chains = 1L,
  sigma2 = c("auto", "random", "fixed"),
  marginal = c("exact", "diagonal"),
  init = "dispersed",
  a0 = 0.001,
  b0 = 0.001,
  seed = NULL,
  progress = interactive()
)
}
\arguments{
\item{object}{a \link{ph_data} object.}

\item{alpha}{the Dirichlet Process concentration parameter.}

\item{mu0, sigma0_sq}{mean and variance of the Gaussian base measure. \code{NULL}
uses the data-scaled defaults described above.}

\item{iters, burn, thin}{sweeps per chain, burn-in to discard, and thinning
interval.}

\item{chains}{number of chains. More than one enables the convergence
diagnostics of \code{\link[=ph_rhat]{ph_rhat()}}; chains start from dispersed partitions.}

\item{sigma2}{\code{"auto"} (default) draws the error variance each sweep when
the object carries a micro panel and conditions on the supplied covariance
otherwise. \code{"random"} and \code{"fixed"} force the choice; asking for
\code{"random"} without a micro panel is not possible and falls back with a
message.}

\item{marginal}{\code{"exact"} (default) evaluates the full collapsed marginal
likelihood, eq. (54), at every cluster-assignment move, so the moves see
the cross-cell covariance. \code{"diagonal"} uses the fast conjugate
normal-normal shortcut that ignores it. Appendix E reports that the
shortcut reproduces the same aggregates but can misstate individual
co-clustering probabilities by up to 0.39, so prefer the default unless
you are deliberately reproducing the shortcut.}

\item{init}{starting partition: \code{"dispersed"} (varies by chain),
\code{"singletons"}, \code{"pooled"}, \code{"random"}, or an explicit label vector.}

\item{a0, b0}{shape and rate of the inverse-gamma prior on \eqn{\sigma^2},
eq. (28). Used only when \code{sigma2 = "random"}.}

\item{seed}{optional integer for reproducibility.}

\item{progress}{print a progress bar.}
}
\value{
An object of class \code{bayes_ph} with components \code{tau} (the posterior
mean cohort-time effects, eq. 38), \code{lower} and \code{upper} (2.5th and 97.5th
posterior percentiles), \code{coclust} (the K x K posterior co-clustering
matrix of eq. 39), \code{m_mean} (the posterior expected number of groups), and
\code{draws} (the retained draws, for \code{\link[=aggregate.bayes_ph]{aggregate.bayes_ph()}} and the
diagnostics).
}
\description{
Places a Dirichlet Process mixture prior on the cohort-time effects and
samples the posterior with the collapsed Gibbs sampler of Neal (2000,
Algorithm 3). Because draws from a Dirichlet Process are almost surely
discrete, realisations put positive probability on ties among the
\eqn{\tau_k}, and those ties \emph{are} the partition. The prior assigns positive
probability to every partition of the \eqn{K} cells while favouring
parsimonious groupings, and never fixes their number.
}
\details{
This is the estimator the paper recommends for inference. Unlike
\code{\link[=l0_ph]{l0_ph()}}, which commits to one partition and reports intervals conditional
on it, the posterior here averages over partitions, so the reported
uncertainty includes uncertainty about the grouping itself. In the paper's
simulations that distinction is decisive: where the partition is uncertain,
the \eqn{\ell_0} plug-in intervals collapse to 0.57--0.62 coverage while
these credible intervals degrade gracefully to 0.79--0.81, and in the
well-separated regime they attain near-nominal 0.93--0.94.
}
\section{Priors}{

The concentration \code{alpha} controls the prior expected number of groups
through eq. (23), \eqn{E[m] \approx \alpha \log(1 + K/\alpha)}; small
\code{alpha} favours pooling, large \code{alpha} favours the flexible fit. Use
\code{\link[=alpha_for_groups]{alpha_for_groups()}} to solve that relation for a target. The paper's
simulations use \code{alpha = 7} at \eqn{K = 18} (so \eqn{E[m] \approx K/2}) and
its applications use \code{alpha = 1}.

The base measure is \eqn{G_0 = N(\mu_0, \sigma_0^2)}. The defaults are
data-scaled, \code{mu0 = median(tau)} and \code{sigma0_sq = (10 * sd(tau))^2},
matching the paper's applications and keeping the prior diffuse on whatever
scale the outcome happens to have. Note that a data-scaled base measure is
mildly empirical-Bayes; supply fixed values if you want a prior that is
genuinely independent of the data.
}

\section{How much the answer depends on alpha}{

Not much for aggregates, and potentially a lot for the grouping when
\eqn{K} is small. The paper's solution paths are nearly flat in \code{alpha} for
the overall ATT in the simulation, but its first application, with only
seven cells, sees the overall effect slide from about -0.017 to -0.038
across \code{alpha}. Rather than fix a value, report the path: see
\code{\link[=alpha_sensitivity]{alpha_sensitivity()}}.
}

\section{On the error variance}{

When the object came from a micro panel, \eqn{\sigma^2} is a real parameter
and \code{sigma2 = "random"} draws it from its inverse-gamma full conditional
each sweep, which is the paper's primary specification. When the object
came from a first-stage estimator, the covariance \eqn{\hat\Sigma} is a
fixed plug-in and there is no scalar variance left to sample, so the
sampler conditions on it (Appendix C). Appendix D shows the two treatments
give the same coverage and interval length to two decimals, so nothing
hinges on this.
}

\examples{
d <- ph_data(c(0.10, 0.11, 0.42, 0.40), Sigma = 0.02^2 * (0.3 + 0.7 * diag(4)))
fit <- bayes_ph(d, alpha = 1, iters = 400, burn = 100, seed = 1)
fit

# The co-clustering matrix is the honest summary of the grouping: cells 1
# and 2 recur together, and so do 3 and 4.
round(coclustering(fit), 2)
}
\references{
Arora, P. and Wagle, R. (2026). Section 3.
See \code{citation("phdid")} for the full reference.

Neal, R. M. (2000). Markov Chain Sampling Methods for Dirichlet Process
Mixture Models. \emph{JCGS} 9(2), 249--265.
}
\seealso{
\code{\link[=aggregate.bayes_ph]{aggregate.bayes_ph()}} for partition-aware aggregate estimands,
\code{\link[=coclustering]{coclustering()}} for the grouping structure, \code{\link[=alpha_sensitivity]{alpha_sensitivity()}} for the
prior path.
}
